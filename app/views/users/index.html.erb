<h1>All Users</h1>

<table border="1" cellpadding="8">
  <tr>
    <th>Name</th>
    <th>Favourites Count</th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user_path(user) %></td>
      <td><%= user.breeds.count %></td>
    </tr>
  <% end %>
</table>
